# ZecDev Launchpad - Main Docker Compose Configuration
# Milestone 1: Zebra-only devnet setup

services:
  # Zebra regtest node
  zebra:
    extends:
      file: docker/compose/zebra.yml
      service: zebra
  
  # Faucet service - PLACEHOLDER for M2
  # Uncomment when implementing faucet in Milestone 2
  # faucet:
  #   extends:
  #     file: docker/compose/faucet.yml
  #     service: faucet
  #   depends_on:
  #     zebra:
  #       condition: service_healthy

# Networks must be defined in main compose file when using extends
networks:
  zecdev:
    driver: bridge
    name: zecdev-network

# Volumes must be defined in main compose file when using extends
volumes:
  zebra-data:
    name: zecdev-zebra-data